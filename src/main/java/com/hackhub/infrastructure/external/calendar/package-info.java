/**
 * Calendar integration adapter layer.
 *
 * <p>Strategy Pattern: application services depend on {@code CalendarClient}
 * and can swap implementations (fake or real provider) without changing business logic.</p>
 */
package com.hackhub.infrastructure.external.calendar;
